// Re-export shared MFA dialog styles
export {
  StyledDialog,
  StyledHeader,
  StyledTitle,
  StyledContent,
  StyledDescription,
  StyledInputContainer,
  StyledInput,
  StyledButtonContainer,
  StyledButton,
  StyledErrorMessage,
} from './MFADialog.styles';
