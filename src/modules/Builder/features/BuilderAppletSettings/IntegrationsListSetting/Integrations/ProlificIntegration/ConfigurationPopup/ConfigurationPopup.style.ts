import styled from '@emotion/styled';
import { Box, Button } from '@mui/material';

export const StyledApiInputWithButton = styled(Box)`
  position: relative;
  width: '100%';
`;

export const StyledApiTokenButton = styled(Button)`
  position: absolute;
  right: 0;
  top: 0;
  height: 100%;
  borderradius: 0;
`;
