import { styled } from '@mui/material';

import { theme } from 'shared/styles';

export const StyledPhrasalTemplateList = styled('ul')({
  listStyleType: 'none',
  margin: 0,
  padding: theme.spacing(1.6, 0, 0),
});
