import { styled } from '@mui/material';

import { StyledFlexTopCenter, variables } from 'shared/styles';

export const StyledLabel = styled(StyledFlexTopCenter)`
  .MuiSvgIcon-root {
    fill: ${variables.palette.primary};
  }
`;
