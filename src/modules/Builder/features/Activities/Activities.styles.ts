import { styled } from '@mui/material';

import { StyledBuilderBtn } from 'shared/styles';

export const StyledAddActivityBtn = styled(StyledBuilderBtn)`
  position: absolute;
  top: 2.4rem;
  right: 6.4rem;
  z-index: 1100;
`;
