export const DEFAULT_SLIDER_MIN_NUMBER = 0;
export const DEFAULT_SLIDER_MAX_NUMBER = 12;
export const DEFAULT_SLIDER_MAX_VALUE = 5;
export const DEFAULT_NUMBER_SELECT_MIN_VALUE = 0;
export const DEFAULT_NUMBER_SELECT_MAX_VALUE = 1;
export const DEFAULT_MIN_NUMBER = 1;
//TODO: remove REACT_HOOK_FORM_KEY_NAME in all places where it was used after release the v.8.0.0 react-hook-form
export const REACT_HOOK_FORM_KEY_NAME = '_id';

export const observerStyles = {
  position: 'absolute',
  height: 'calc(100% + 20rem)',
  bottom: 0,
};

export const ITEMS_COUNT_TO_ACTIVATE_STATIC = 20;
