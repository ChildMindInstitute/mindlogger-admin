export type Breadcrumb = {
  icon?: string;
  image?: string;
  useCustomIcon?: boolean;
  label: string;
  chip?: string;
  navPath?: string;
  disabledLink?: boolean;
  hasUrl?: boolean;
  key?: string;
};
